const numbers = [5, 8, 10, 20];
let max = numbers[0];
for (let i = 1; i < numbers.length; i++) {
  if (max! < numbers[i]!) {
    max = numbers[i];
  }
}
console.log("max is : ", max);
// total time neded is 5 min thingking and coding
